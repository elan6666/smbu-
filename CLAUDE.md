# CLAUDE.md

This repository contains the SMBU admissions dialogue system course project. Start with `AGENTS.md` and `.byte-os/STATUS.md`. Keep edits scoped, preserve provenance for admissions data, and verify with `pytest`, `python -m compileall app scripts`, ingestion, training, evaluation, and LaTeX compilation when relevant.

